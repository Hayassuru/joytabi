<%= stylesheet_link_tag 'index', :media => "all" %>

<div class="travels-container">
<h3>投稿一覧</h3>
  <% @travels.each do |t| %>
    <div class="travel">
      <div class="main-box">
        <div class="left-container"><%= t.body %></div>
        <div class="right-container">
          <%= link_to "詳細", travel_path(t.id) %>
          <%= link_to "編集", edit_travel_path(t.id) %>
          <%= link_to "削除", travel_path(t.id), method: :delete %>
        </div>
      </div>
        <p class="time"><%= t.created_at %></p>
    </div>
  <% end %>
</div>